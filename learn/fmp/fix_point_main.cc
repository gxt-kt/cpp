#include "fix_point.h"
#include "common.h"


// using type=fpml::fixed_point<std::int32_t,16>;

int main (int argc, char *argv[]) {
  gDebug() << G_FILE;
  // type a=0.1;
  // type b=0.1;
  // a+b;
  // gDebug(a-b);
  // gDebug(a*b);
  // gDebug(a/b);
  // a+b;
  // gDebug(a/b);
  // gDebug(a+b);
  // gDebug(a-b);
  // gDebug(a*b);
  return 0;
}
